// Code generated by go-featureprocessing DO NOT EDIT

package main

import (
	"encoding/json"
	"testing"

	"github.com/google/gofuzz"
	"github.com/stretchr/testify/assert"
)

// makeMock creates some valid PassengerFeatureTransformer by fitting on fuzzy data.
// This function is handy for tests.
func makeMockPassengerFeatureTransformer() *PassengerFeatureTransformer {
	s := make([]Passenger, 10)
	fuzz.New().NilChance(0).NumElements(10, 10).Fuzz(&s)

	tr := PassengerFeatureTransformer{}
	tr.Fit(s)
	return &tr
}

func TestPassengerFeatureTransformerFeatureNames(t *testing.T) {
	tr := makeMockPassengerFeatureTransformer()

	t.Run("feature names", func(t *testing.T) {
		names := tr.FeatureNames()
		assert.True(t, len(names) > 0)
		assert.Equal(t, len(names), tr.NumFeatures())
	})

	t.Run("feature name transformer is empty", func(t *testing.T) {
		tr := PassengerFeatureTransformer{}
		names := tr.FeatureNames()
		assert.True(t, len(names) > 0)
		assert.Equal(t, len(names), tr.NumFeatures())
	})

	t.Run("feature name transformer is nil", func(t *testing.T) {
		var tr *PassengerFeatureTransformer
		names := tr.FeatureNames()
		assert.Nil(t, names)
	})
}

func TestPassengerFeatureTransformerTransform(t *testing.T) {
	tr := makeMockPassengerFeatureTransformer()

	t.Run("empty struct", func(t *testing.T) {
		s := Passenger{}
		features := tr.Transform(&s)

		assert.NotNil(t, features)
		assert.True(t, len(features) > 0)
		assert.Equal(t, tr.NumFeatures(), len(features))
	})

	t.Run("fuzzy struct", func(t *testing.T) {
		var s Passenger
		fuzz.New().Fuzz(&s)

		tr := PassengerFeatureTransformer{}
		fuzz.New().NilChance(0).NumElements(1, 1).Fuzz(&tr)

		features := tr.Transform(&s)

		assert.NotNil(t, features)
		assert.True(t, len(features) > 0)
		assert.Equal(t, tr.NumFeatures(), len(features))
	})

	t.Run("struct is nil", func(t *testing.T) {
		var s *Passenger
		features := tr.Transform(s)
		assert.Nil(t, features)
		assert.True(t, tr.NumFeatures() > 0)
	})

	t.Run("transformer is nil", func(t *testing.T) {
		var s Passenger
		fuzz.New().Fuzz(&s)

		var tr *PassengerFeatureTransformer
		features := tr.Transform(&s)

		assert.Nil(t, features)
		assert.Equal(t, tr.NumFeatures(), 0)
	})

	t.Run("serialize and deserialize transformer", func(t *testing.T) {
		output, err := json.Marshal(tr)
		assert.Nil(t, err)
		assert.NotEmpty(t, output)

		var tr2 PassengerFeatureTransformer
		err = json.Unmarshal(output, &tr2)
		assert.Nil(t, err)
		assert.Equal(t, *tr, tr2)
	})

	t.Run("inplace transform does not run when destination does not match num features", func(t *testing.T) {
		var s Passenger
		fuzz.New().Fuzz(&s)

		tr := PassengerFeatureTransformer{}

		features := make([]float64, 1000)
		features[0] = 123456789.0
		tr.TransformInplace(features, &s)

		assert.Equal(t, 123456789.0, features[0])
	})
}

func TestPassengerFeatureTransformerTransformAll(t *testing.T) {
	t.Run("when transformer is nil", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		dst := make([]float64, 100*100)

		var tr *PassengerFeatureTransformer
		assert.Nil(t, tr.TransformAll(s))
		assert.Nil(t, tr.TransformAllParallel(s, 4))

		// does not panic
		tr.TransformAllInplace(dst, s)
		tr.TransformAllInplaceParallel(dst, s, 4)
	})

	t.Run("inplace with wrong output dimensions, output is smaller", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		dst := make([]float64, 100)

		tr := makeMockPassengerFeatureTransformer()

		// does not panic
		tr.TransformAllInplace(dst, s)
		tr.TransformAllInplaceParallel(dst, s, 4)
	})

	t.Run("inplace with wrong output dimensions, output is bigger", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		dst := make([]float64, 100*120)

		tr := makeMockPassengerFeatureTransformer()

		// does not panic
		tr.TransformAllInplace(dst, s)
		tr.TransformAllInplaceParallel(dst, s, 4)
	})

	t.Run("transform all", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		tr := makeMockPassengerFeatureTransformer()

		features := tr.TransformAll(s)
		assert.Equal(t, len(s)*tr.NumFeatures(), len(features))
	})

	t.Run("transform all parallel 1 worker", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		tr := makeMockPassengerFeatureTransformer()

		features := tr.TransformAllParallel(s, 1)
		assert.Equal(t, len(s)*tr.NumFeatures(), len(features))
	})

	t.Run("transform all parallel 4 workers", func(t *testing.T) {
		s := make([]Passenger, 100)
		fuzz.New().NilChance(0).NumElements(100, 100).Fuzz(&s)

		tr := makeMockPassengerFeatureTransformer()

		features := tr.TransformAllParallel(s, 4)
		assert.Equal(t, len(s)*tr.NumFeatures(), len(features))
	})
}

func TestPassengerFeatureTransformerFit(t *testing.T) {
	t.Run("fuzzy input", func(t *testing.T) {
		s := make([]Passenger, 10)
		fuzz.New().NilChance(0).NumElements(1, 1).Fuzz(&s)

		trEmpty := PassengerFeatureTransformer{}
		tr := PassengerFeatureTransformer{}
		tr.Fit(s)

		assert.NotNil(t, tr)
		assert.NotEqual(t, tr, trEmpty)
	})

	t.Run("not nil transformer nil input", func(t *testing.T) {
		trEmpty := PassengerFeatureTransformer{}
		tr := PassengerFeatureTransformer{}
		tr.Fit(nil)

		assert.Equal(t, trEmpty, tr)
	})

	t.Run("nil transformer not nil input", func(t *testing.T) {
		s := make([]Passenger, 10)

		var tr *PassengerFeatureTransformer
		tr.Fit(s)

		assert.Nil(t, tr)
	})
}

func fitTransformerPassenger(b *testing.B, numelem int) {
	s := make([]Passenger, numelem)
	fuzz.New().NilChance(0).NumElements(numelem, numelem).Fuzz(&s)

	var tr PassengerFeatureTransformer

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.Fit(s)
	}
}

func BenchmarkPassengerFeatureTransformer_Fit_100elements(b *testing.B) {
	fitTransformerPassenger(b, 100)
}

func BenchmarkPassengerFeatureTransformer_Fit_1000elements(b *testing.B) {
	fitTransformerPassenger(b, 1000)
}

func BenchmarkPassengerFeatureTransformer_Fit_10000elements(b *testing.B) {
	fitTransformerPassenger(b, 10000)
}

func BenchmarkPassengerFeatureTransformer_Transform(b *testing.B) {
	var s Passenger
	fuzz.New().Fuzz(&s)

	tr := makeMockPassengerFeatureTransformer()

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.Transform(&s)
	}
}

func BenchmarkPassengerFeatureTransformer_Transform_Inplace(b *testing.B) {
	var s Passenger
	fuzz.New().Fuzz(&s)

	tr := makeMockPassengerFeatureTransformer()

	features := make([]float64, tr.NumFeatures())

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.TransformInplace(features, &s)
	}
}

func benchTransformAllPassenger(b *testing.B, numelem int) {
	s := make([]Passenger, numelem)
	fuzz.New().NilChance(0).NumElements(numelem, numelem).Fuzz(&s)

	tr := makeMockPassengerFeatureTransformer()

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.TransformAll(s)
	}
}

func BenchmarkPassengerFeatureTransformer_TransformAll_10elems(b *testing.B) {
	benchTransformAllPassenger(b, 10)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_100elems(b *testing.B) {
	benchTransformAllPassenger(b, 100)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_1000elems(b *testing.B) {
	benchTransformAllPassenger(b, 1000)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_10000elems(b *testing.B) {
	benchTransformAllPassenger(b, 10000)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_100000elems(b *testing.B) {
	benchTransformAllPassenger(b, 100000)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_1000000elems(b *testing.B) {
	benchTransformAllPassenger(b, 1000000)
}

func benchTransformAllParallelPassenger(b *testing.B, numelem int, nworkers uint) {
	s := make([]Passenger, numelem)
	fuzz.New().NilChance(0).NumElements(numelem, numelem).Fuzz(&s)

	tr := makeMockPassengerFeatureTransformer()

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.TransformAllParallel(s, nworkers)
	}
}

func BenchmarkPassengerFeatureTransformer_TransformAll_10elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 10, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_100elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 100, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_1000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 1000, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_10000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 10000, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_100000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 100000, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_1000000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 1000000, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_5000000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 5000000, 8)
}

func BenchmarkPassengerFeatureTransformer_TransformAll_15000000elems_8workers(b *testing.B) {
	benchTransformAllParallelPassenger(b, 15000000, 8)
}

func benchLargeTransformerPassenger(b *testing.B, numelem int) {
	var s []Passenger
	fuzz.New().NilChance(0).NumElements(numelem, numelem).Fuzz(&s)

	tr := PassengerFeatureTransformer{}
	tr.Fit(s)

	b.ResetTimer()
	for n := 0; n < b.N; n++ {
		tr.Transform(&s[0])
	}
}

func BenchmarkPassengerFeatureTransformer_Transform_LargeComposites_100elements(b *testing.B) {
	benchLargeTransformerPassenger(b, 100)
}

func BenchmarkPassengerFeatureTransformer_Transform_LargeComposites_1000elements(b *testing.B) {
	benchLargeTransformerPassenger(b, 1000)
}

func BenchmarkPassengerFeatureTransformer_Transform_LargeComposites_10000elements(b *testing.B) {
	benchLargeTransformerPassenger(b, 10000)
}

func BenchmarkPassengerFeatureTransformer_Transform_LargeComposites_100000elements(b *testing.B) {
	benchLargeTransformerPassenger(b, 100000)
}
